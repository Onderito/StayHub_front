import React from "react";
import Input from "../utils/InputComponent";

const HeroSection = () => {
  return (
    <div className="flex flex-col md:flex-row md:gap-4 justify-center items-center  mt-24">
      <Input />
    </div>
  );
};

export default HeroSection;
